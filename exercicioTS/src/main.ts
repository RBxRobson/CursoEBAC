const multiplicar = (x: number, y: number) => x * y;
const conta1 = multiplicar(10, 20);
console.log(conta1);

const concatenar = (nome: string) => `Olá ${nome}`;
const saudação1 = concatenar('Robson')
console.log(saudação1);